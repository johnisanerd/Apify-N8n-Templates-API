[project]
name = "apify-n8n-templates-api-example"
version = "0.1.0"
description = "Python example for the n8n templates API on Apify: search the n8n workflow library and export workflow JSON."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
